// const {Hand} = require('./Hand.js')

test('createDeck creates and calls shuffle', () => {
  // const hand = new Hand({id: 0, playerService: new PlayerService()})
  // console.log(hand);
  expect(2).toBe(2);
});
